require(['models/main', 'views/fade', 'controllers/main'], function(models, views, controllers) {

    mvc({
    
        models: models,
        views: views,
        controllers: controllers

    });

});
